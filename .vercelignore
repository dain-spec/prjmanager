# 배포에 불필요한 파일 — Vercel 은 루트의 모든 정적 파일을 URL 로 노출하므로
# 런타임에 쓰이지 않는 파일은 반드시 제외한다.

# 사내 디자인 시스템 원본. src/tokens.css 로 이미 변환돼 있어 런타임에 불필요하다.
# 포함하면 https://<도메인>/wehago.token.json 으로 누구나 내려받을 수 있다.
wehago.token.json

# 빌드/개발 전용
tools/
.claude/
README.md
